import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    root: {
        margin: 10
    },
    title: {
        fontSize: 20,
        color: "black",
    },
    loader: {
        color: "green",
        alignSelf: "center",
        justifySelf: "center",
    }
});

export default useStyles